package com.boardgamegeek.events

class GameSelectedEvent(
        val id: Int,
        val name: String,
        val imageUrl: String,
        val thumbnailUrl: String,
        val heroImageUrl: String)
